{
  "type": "choice",
  "nodetype": "URI",
  "property": "dcterms:type",
  "label": {
    "en": "Architectural style",
    "sv": "Arkitekturstil"
  },
  "description": {
    "en": "Which architectural style used for the service. Use dcterms:conformsTo to indicate more details such as the exact protocol used.",
    "sv": "Vilken arkitekturstil som tjänsten använder. Använd dcterms:conformsTo för att indikera mer detaljer som vilken exakt vilket protokoll som används."
  },
  "choices": [
    {
      "value": "http://www.wikidata.org/entity/Q749568",
      "label": {
        "en": "REST",
        "sv": "REST"
      },
      "description": {
        "en": "RESTful services makes use of different URIs (resources) and the stateless operations provided in the HTTP protocol, e.g. GET, PUT, POST and DELETE to access and manipulate data.",
        "sv": "Tjänster baserade på principerna bakom REST använder sig av olika URI:er och de tillståndslösa operationerna i HTTP protokollet, t.ex. GET, PUT, POST och DELETE för att komma åt och manipulera data."
      }
    },
    {
      "value": "http://www.wikidata.org/entity/Q62270",
      "label": {
        "en": "RPC",
        "sv": "RPC"
      },
      "description": {
        "en": "RPC services (Remote Procedure Call) implements a client-service message passing model allowing different procedures to be called from a single endpoint. RPC services can be divided into categories based on which protocol they use, the most common categories are XML-RPC, JSON-RPC, SOAP, and gRPC.",
        "sv": "RPC tjänster (Remote Procedure Call) implmementerar en klient-server modell för meddelande transport där olika metoder kan anropas från samma åtkomstadress. RPC tjänster kan delas in i olika kategorier baserat på vilket protokoll de använder, de vanligaste protokollen är XML-RPC, JSON-RPC, SOAP och gRPC."
      }
    }
  ]
}